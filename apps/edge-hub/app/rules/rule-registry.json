[
  {
  "id": "open_when_gap",
  "enabled": true,
  "priority": 100,
  "condition": "machine['gap_transition'] == 'enter'",
  "cooldown_ms": 0,
  "actions": [
    {
      "type": "open_valve",
      "params": { "valve_id": 1 }
    }
    ]
  },

  {
    "id": "stop_on_high_pressure",
    "enabled": true,
    "priority": 150,
    "condition": "machine.get('pressure', 0) > 100",
    "actions": [
      { "type": "stop_program" }
    ]
  }
]

